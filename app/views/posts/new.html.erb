<h1>New Post</h1>

<%= simple_form_for @post do |f| %>
  <%= f.input :title, placeholder: "Post #{current_user.posts.count + 1}" %>
  <%= f.input :text, placeholder: "Post Content" %>
  <%= f.button :submit %>
<% end %>